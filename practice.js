var arr = [1,2,3,4,5];

function square(arr){
    var length = arr.length();
    var newarr = []

    for(int i = 0; i < length; i++){
        new[i] = arr[i]*arr[i];
    }

    return newarr;
}


arr.map(x=>x*x);

